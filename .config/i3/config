# This file has been auto-generated by i3-config-wizard

set $mod Mod1

font pango:monospace 8

exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +2% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -2% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use brightnessctl to set brigthness level
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +5%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%-

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
bindsym Ctrl+$mod+T exec --no-startup-id alacritty

# kill focused window
bindsym $mod+Escape kill

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+i focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
#bindsym $mod+Left focus left
#bindsym $mod+Down focus down
#bindsym $mod+Up focus up
#bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+i move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace
bindsym $mod+Shift+r exec killall "i3-layouts" && i3-msg restart && i3-layouts & $HOME/.config/polybar/launch.sh --panels 

for_window [class=".*"] border pixel 3

# class                 border  backgr. text    indicator child_border
client.focused          #039be5 #039be5 #ffffff #039be5   #039be5
client.unfocused        #221D2F #221D2F #ffffff #221D2F   #221D2F
client.urgent           #221D2F #221D2F #ffffff #221D2F   #221D2F
client.placeholder      #221D2F #221D2F #ffffff #221D2F   #221D2F
client.focused_inactive #221D2F #221D2F #ffffff #221D2F   #221D2F

border_radius 5

gaps inner 8
gaps outer 4

smart_gaps inverse_outer

smart_borders on
smart_borders no_gaps

exec --no-startup-id feh --bg-scale ~/Downloads/wp9034206-pink-lofi-wallpapers.png
exec --no-startup-id python $HOME/Scripts/i3-hud-menu/hud-menu-service.py
exec --no-startup-id $HOME/.config/polybar/launch.sh --panels
exec --no-startup-id setxkbmap -layout it
exec --no-startup-id thunar --daemon
exec --no-startup-id i3-layouts
exec --no-startup-id xbindkeys
exec --no-startup-id kwallet5
exec --no-startup-id cbatticon
exec --no-startup-id yakuake

## rofi scripts key bindings
bindsym Ctrl+$mod+x exec --no-startup-id $HOME/Scripts/rofi/rofi-tmuxinator.sh
bindsym Ctrl+$mod+Return exec --no-startup-id $HOME/Scripts/rofi/rofi-path-launcher.sh
bindsym $mod+Return exec --no-startup-id $HOME/Scripts/rofi/rofi-chromium-bookmarks.sh -c ~/.config/i3/scripts/mv_to_win_ws.sh chromium  
bindsym Shift+Return exec --no-startup-id $HOME/Scripts/rofi/rofi-window-switcher.sh
bindsym Ctrl+Shift+Return exec --no-startup-id $HOME/Scripts/rofi/rofi-todo-list.sh
bindsym $mod+Shift+e exec --no-startup-id $HOME/Scripts/rofi/rofi-system-menu.sh
#bindsym Super exec --no-startup-id $HOME/.config/polybar/panels/scripts/launcher.sh --budgie

## keybinds
bindsym Ctrl+$mod+b exec --no-startup-id chromium
bindsym $mod+Tab workspace next
bindsym Ctrl+$mod+Right workspace next
bindsym Ctrl+$mod+Left workspace prev
bindsym Ctrl+$mod+m exec --no-startup-id python $HOME/Scripts/i3-hud-menu/hud-menu.py dmenu

## new ws
bindsym Ctrl+$mod+Down exec --no-startup-id $HOME/.config/i3/scripts/new_ws.sh

## tidy ws
bindsym Ctrl+$mod+space exec --no-startup-id $HOME/.config/i3/scripts/tidy.sh

## gnome keyring
exec --no-startup-id dbus-update-activation-environment --all

## screen capturing
bindsym Stamp exec --no-startup-id spectacle

## i3 layouts
bindsym Ctrl+$mod+1 exec --no-startup-id i3l vstack
bindsym Ctrl+$mod+2 exec --no-startup-id i3l hstack
bindsym Ctrl+$mod+3 exec --no-startup-id i3l spiral
bindsym Ctrl+$mod+4 exec --no-startup-id i3l 2columns
bindsym Ctrl+$mod+5 exec --no-startup-id i3l 3columns
bindsym Ctrl+$mod+6 exec --no-startup-id i3l companion
bindsym Ctrl+$mod+7 exec --no-startup-id i3l autosplit

## lock screen shortcut
bindsym Control+Mod1+l exec --no-startup-id "i3lock -c 000000"

# auto lock the screen
exec --no-startup-id "xautolock -detectsleep -time 3 -locker \"i3lock -c 000000\""